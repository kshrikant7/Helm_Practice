# backend-chart/values.yaml
replicaCount: 1

image:
  repository: shrikantk7/backendtest
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  name: http
  protocol: TCP
  targetPort: 5000
  port: 5000
  containerPort: 5000
  nodePort: 30002

serviceAccount:
  create: false

ingress:
  enabled: false

autoscaling:
 enabled: false

resources: {}